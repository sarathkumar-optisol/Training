<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.training">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Training">
        <activity android:name=".Camera2"></activity>
        <activity android:name=".CameraActivity" />
        <activity android:name=".RealmActivity" />
        <activity android:name=".Database" />
        <activity android:name=".FileSystem" />
        <activity android:name=".CustomFont" />
        <activity android:name=".CustomStyleForViews" />
        <activity android:name=".CustomShapes" />
        <activity android:name=".CustomShapes2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>